{#
  macro: generate_surrogate_key
  purpose: Deterministic surrogate key for composite-grain models.
  limitations: Key inputs must match the model's declared grain exactly.
  STATUS: NOT IMPLEMENTED. Phase 0 scaffold.
#}
